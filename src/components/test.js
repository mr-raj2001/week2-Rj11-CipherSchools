import React, { Component } from "react";

/* const Test = () => {
    return(
        <div>
            <h1>Hello i am Rishav</h1>
        </div>
    );
}; */

class Test extends Component{
    render() {
        return (
        <div>
<h1> hello i am raj</h1>
        </div>

            
        );
    }
}

export default Test;

//how to use map

/* render( ){
    return(
        <div>
            {this.allusers.map{(user,index) => (
                <userdetails keys={index} user=user} />
            )}}
        </div>
    )
} */